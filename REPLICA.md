# 副看板隔离规则

- 本目录仅服务 `http://127.0.0.1:8766/`。
- 运行配置和数据只保存在 `instances/test/`。
- 副本修复只在本目录修改和提交，禁止回写主看板目录。
- 主看板已确认的通用修复可以单向同步到本目录，并在本仓库单独提交。
- `http://127.0.0.1:8765/` 始终由 `hyperframes-dashboard/` 提供。
